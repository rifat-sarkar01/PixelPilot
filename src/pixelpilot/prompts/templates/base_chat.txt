VISUAL PLAN FROM VISION MODEL
------------------------------
$visual_plan
(If a plan is present above, implement it directly - it was produced by looking
at the actual current canvas, so its sizes/positions/colors are already grounded
and correct. Treat it as the specification; the user request is the intent behind it.)

CURRENT CANVAS STATE
--------------------
$canvas_state

RELEVANT API PROCEDURES
-----------------------
$procedures

FEW-SHOT EXAMPLE (syntax and API style only)
---------------------------------------------
$example
(The example's request and code are reference material only. Never reuse its
task, docstring, operations, or subject unless they are explicitly requested
below.)

CONVERSATION HISTORY
--------------------
$history

CURRENT USER REQUEST — AUTHORITATIVE
------------------------------------
$current_request
Write code for this request only. It overrides any different task mentioned in
the visual plan, API reference, example, or conversation history.
